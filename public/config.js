/* ============================================================
   config.js – Orario Treni
   Imposta API_BASE con l'URL del backend (vuoto = stesso host).
   Per il deploy su GitHub Pages, sostituire con l'URL di Render:
     window.API_BASE = 'https://<nome-app>.onrender.com';
   ============================================================ */

window.API_BASE = '';
